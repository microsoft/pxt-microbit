/**
 * Provides access to basic micro:bit functionality.
 */
//% color=#0078D7 weight=100
namespace basic {
    /**
     * Code that runs when the program starts.
     */
    //% blockId=pxtStart block="on start" weight=10
    export function onStart(handler: Action) {
        // this is just a stub, should not be used
        handler();
    }
}
